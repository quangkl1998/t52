import React from "react";
import { Outlet } from "react-router-dom";

const MortgageLoanTemplate = () => {
    return (
        <div>
            <Outlet />
        </div>
    );
};

export default MortgageLoanTemplate;
